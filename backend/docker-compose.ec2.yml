version: "3.9"

services:
  api:
    container_name: reddit-dashboard-api
    build:
      context: ..
      dockerfile: backend/Dockerfile
    ports:
      - "80:8000"
    env_file:
      - .env
    restart: unless-stopped
